<h1> All Users </h1>

<ul>
Usernames:
    <% @users.each do |user| %>
        <li>
        <%= link_to user.username, user_path(user) %>
        </br>
        Posts:
        <%= user.pictures.count %>
        </li>
        
    <% end %>
    </br>
    <%= button_to "All Pictures", pictures_path, method: "get" %>
</ul>